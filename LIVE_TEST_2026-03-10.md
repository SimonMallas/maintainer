# Maintainer Live Test Report — 2026-03-10

## Window
- Duration: 45s controlled run
- Mode: remediation dry-run (default)
- Probe env:
  - `SENTINEL_HEALTH_URL=http://127.0.0.1:8085/healthz`
  - `SENTINEL_PROM_URL=http://127.0.0.1:9090/metrics`
  - `SENTINEL_PROM_MATCH=process_start_time_seconds`

## Observed module outputs
- process: `critical` (service `openclaw` reported inactive)
- remediation: `skipped` (cooldown active; no restart executed)
- gpu: `ok`
- memory: `warn` (psutil not installed)
- disk: `ok`
- config_drift: `ok`
- sentinel_probe: `ok`

## Interpretation
- Sentinel probe path is functioning in live mode.
- Guardrail path is functioning (critical -> remediation decision logged without unsafe loop).
- Two host readiness issues before wider rollout:
  1) process service target mismatch/availability (`openclaw` inactive via systemd check)
  2) memory module dependency missing (`psutil`)

## Follow-up run (pass 2)
- Created `maintainer/.venv` and installed `psutil`.
- Re-ran 45s live test with host-profile module set:
  - `MAINTAINER_ENABLED_MODULES=gpu,memory,disk,config_drift,sentinel_probe`
  - same Sentinel probe env overrides as above
- Result: **all module outputs were `ok`** for sampled interval.

## Monitoring note correction
- Prometheus target label is `job="sentinel-control-plane"` on this host.
- Earlier check using `job="sentinel"` can produce false-down signals.

## Next actions
1) Decide host profile for process check (systemd service target vs non-systemd process probe).
2) Run extended 15–30 minute live window with webhook capture enabled.
3) If stable, promote to default runtime profile.
